VITE_API_URL=https://rms-render-atlas-1-pf0i.onrender.com

# If your API routes are mounted under /api, use the following instead:
# VITE_API_URL=https://rms-render-atlas-1-pf0i.onrender.com/api

# Note: Vite exposes variables prefixed with VITE_ to the client. Do NOT put secrets
# (API keys, DB passwords) here: use Render/Netlify/Cloud provider secret env settings.