VITE_API_URL=https://rms-render-atlas-1.onrender.com/api

# If you prefer to point to the host root instead (not including /api), use:
# VITE_API_URL=https://rms-render-atlas-1.onrender.com

# Note: Variables prefixed with VITE_ are exposed to client code via import.meta.env