# Example local overrides for development. Copy to `.env.local` (gitignored) and edit as needed.
# This file is not for secrets; use `.env.local` for machine-specific overrides.

VITE_API_URL=http://localhost:5000

# Production-like host (example):
# VITE_API_URL=https://rms-render-atlas-1-pf0i.onrender.com