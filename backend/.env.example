# Copy this file to .env and fill in real values
MONGO_URI=mongodb+srv://bhoyarpranav26_db_user:Pranav123%40@cluster0.o90c8g1.mongodb.net/rms?retryWrites=true&w=majority

JWT_SECRET=b9353cb479f160d460839279a1750fa365ba04a28c01b21d7f726ef5232dba2892e166e5bbc5a419fc9285007c06f40e3f71ebb63ceaf6388076e670b4a63e1e

JWT_EXPIRES_IN=7d
PORT=5000

# SMTP settings for sending OTP emails (e.g. Gmail SMTP or any SMTP provider)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_SECURE=True
## IMPORTANT: do NOT commit real secrets. Set these values in your deployment
## provider (Render/Heroku/etc) or in a local .env that is gitignored.
# For Gmail use an App Password (requires 2FA) and put the app password in EMAIL_PASS.
# Example (replace with your real values in the hosting dashboard, not here):
EMAIL_USER=sanketkulkarni469@gmail.com
EMAIL_PASS=rolblxxslnoegycu

# Notes:
# - For SMTP over TLS use port 465 and EMAIL_SECURE=true.
# - For STARTTLS (recommended for smtp.gmail.com) use port 587 and EMAIL_SECURE=false.
# - If you accidentally pasted a real app password in this repo or a chat, revoke it
#   immediately and create a new App Password in your Google Account.


# Optional admin creation key for seeding an admin account (keep secret)
ADMIN_KEY=supersecretadminkey

# If you require SMTP to be present (fail OTP requests when SMTP missing), set:
# EMAIL_REQUIRED=true

# Frontend origin to allow in CORS (set this in Render as FRONTEND_URL)
# FRONTEND_URL=https://your-frontend-service.onrender.com
